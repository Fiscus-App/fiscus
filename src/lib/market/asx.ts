/**
 * DEPRECATED — dead code (no importers anywhere in the app).
 * Superseded by the unified market-data service used by /api/market/summary.
 * Safe to delete: `git rm src/lib/market/asx.ts`
 */
export {}
